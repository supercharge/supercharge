{{#pagination}}

    <nav class="pagination is-centered" role="navigation" aria-label="pagination">
        {{#if prev}}
            <a class="pagination-previous has-white-background" href="{{prev}}">Previous</a>
        {{/if}}

        {{#if next}}
            <a class="pagination-next has-white-background" href="{{next}}">Next</a>
        {{/if}}

        <ul class="pagination-list">
            {{#repeat lastPage start=1 }}
                <li>
                    <span class="pagination-link has-white-background {{#eq @index currentPage}} is-current{{/eq}}"
                        aria-current="page" aria-label="{{#eq @index currentPage}}Page {{else}}Goto page {{@index}}{{/eq}}">{{@index}}</span>
                </li>
            {{/repeat}}
        </ul>
    </nav>

{{/pagination}}
