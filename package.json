{
  "name": "supercharge",
  "description": "Supercharge application boilerplate",
  "version": "1.0.0-beta8",
  "type": "module",
  "dependencies": {
    "@supercharge/contracts": "0.0.0",
    "@supercharge/core": "0.0.0",
    "@supercharge/facades": "0.0.0",
    "@supercharge/http": "0.0.0",
    "@supercharge/routing": "0.0.0",
    "@supercharge/strings": "~1.17.1",
    "@supercharge/view": "0.0.0"
  },
  "devDependencies": {
    "nodemon": "~2.0.7"
  },
  "engines": {
    "node": ">= 12"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "dev": "npm run dev:server",
    "dev:server": "nodemon --experimental-specifier-resolution=node --watch './**/*.js' server.js",
    "test": "echo 'Missing tests.' && exit 0"
  }
}
